Create a iterator for movie and songs.

Now if we want to iterate on the basis of diff things(songs based on artist, ratings etc) we could have create diff iterators implementing our Iterator interface.